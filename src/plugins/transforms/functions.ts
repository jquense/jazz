import {
  alpha,
  blue,
  channel,
  complement,
  darken,
  desaturate,
  grayscale,
  green,
  hue,
  lighten,
  lightness,
  opacify,
  red,
  saturate,
  saturation,
  transparentize,
} from 'khroma';

import * as Ast from '../../parsers/Ast';

// const isColorFunction = (node: Ast.Function) =>
//   node.value

function transformFunction(node: Ast.Function) {}
